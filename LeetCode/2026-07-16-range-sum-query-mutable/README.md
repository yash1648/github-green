# Range Sum Query - Mutable

- **Difficulty**: Medium
- **Source**: [Alt-Leetcode](https://leetcode.com/problems/range-sum-query-mutable/)
- **Date**: 2026-07-16
- **Language**: java


Today, I tackled the "Range Sum Query - Mutable" problem on LeetCode. It's a medium-difficulty question that requires implementing a data structure that can efficiently update and query a range of numbers within an array.

Initially, I struggled with the problem for a while, trying different brute-force approaches. I realized that I needed to use a segment tree or a similar data structure to efficiently update and query the range sum. However, I didn't know how to implement one efficiently.

But then, I stumbled upon a tutorial that explained how to implement a segment tree with O(log n) update and query time complexity. This was a game-changer for me! I followed the tutorial and implemented the code.

The complexity of my solution is O(n) for initialization, O(log n) for update and sumRange operations. The space complexity is O(n) as well, which is not too bad considering the problem's requirements.

In conclusion, this was an interesting problem that required me to learn a new concept. I'm glad I was able to implement a segment tree and solve it, even if it took me some time. I'll remember this problem and the concept of segment tree for the future.